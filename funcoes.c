#include <math.h>
#include "EstruturasDeDados.h"

/// ***********************************************************************
/// ** 
/// ***********************************************************************

float implicitFunc0(tPonto p) {
	
float 	a = 1.5f,
		b = 1.0f;
	
	return ((p.x*p.x) / (a*a) + (p.y*p.y) / (b*b) - 1.0f);
}
/// ***********************************************************************
/// ** 
/// ***********************************************************************

float implicitFunc1(tPonto p) {
	
float 	a = 0.5f,
		b = 1.0f;
	
	return ((p.x*p.x) / (a*a) - (p.y*p.y) / (b*b) - 1.0f);
}

/// ***********************************************************************
/// ** 
/// ***********************************************************************

float implicitFunc2(tPonto p) {
	
float 	a = 1.5f;
	
	return ((p.x*p.x) - 4 * a * p.y - 0.5f);
}

/// ***********************************************************************
/// ** 
/// ***********************************************************************

float implicitFunc3(tPonto p) {
	
float 	a = 1.5f;
	
	return ((p.x*p.x) + (p.y*p.y) + (p.x * p.y) - pow(p.x * p.y, 2) * 0.5f - 0.25f);
}

/// ***********************************************************************
/// ** 
/// ***********************************************************************

float implicitFunc4(tPonto p) {
	
float 	a = 1.5f;
	
	return (pow(p.x,7) - pow(p.y,5) + pow(p.x,2) * pow(p.y,3) - pow(p.x * p.y, 2));
}


